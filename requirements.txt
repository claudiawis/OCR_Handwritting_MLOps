# General dependencies
pandas==2.2.3
numpy==2.0.2  # Likely needed by pandas or your data processing
tqdm==4.67.0  # Progress bar utility used in your script

# NLTK for stopword processing
nltk==3.9.1

# Scikit-learn for Label Encoding
scikit-learn==1.6.0

# DVC and DagsHub for data versioning and repository management
dvc==3.58.0
dvc-http==2.32.0
dagshub<0.4.0

# Cryptography for DVC HTTP operations (if needed for your DagsHub setup)
cryptography>=38.0.4,<45.0.0

# tarfile and os are part of the Python standard library (no need to add these)
